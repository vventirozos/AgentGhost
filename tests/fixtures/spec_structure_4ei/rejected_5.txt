```json
{"files":[{"path":"index.html","append":"    <section class=\"history\">\n      <div class=\"history-header\">\n        <h2>Recent Calculations</h2>\n        <button type=\"button\" id=\"history-clear\" class=\"history-clear\">Clear</button>\n      </div>\n      <ul id=\"history-list\" class=\"history-list\"></ul>\n    </section>"},"path":"styles.css","append":".history {\n  margin-top: 24px;\n  padding: 16px;\n  background: #1e293b;\n  border-radius: 12px;\n}\n\n.history-header {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  margin-bottom: 12px;\n}\n\n.history-header h2 {\n  font-size: 1rem;\n  margin: 0;\n  color: #94a3b8;\n}\n\n.history-clear {\n  background: transparent;\n  border: none;\n  color: #ef4444;\n  cursor: pointer;\n  font-size: 0.85rem;\n  padding: 4px 8px;\n  border-radius: 6px;\n}\n\n.history-clear:hover {\n  background: #334155;\n}\n\n.history-list {\n  list-style: none;\n  margin: 0;\n  padding: 0;\n}\n\n.history-list li {\n  font-size: 0.9rem;\n  color: #cbd5e1;\n  padding: 6px 0;\n  border-bottom: 1px solid #334155;\n}\n\n.history-list li:last-child {\n  border-bottom: none;\n}"},"path":"app.js","edits":[{"find":"    tipAmountEl.textContent = formatMoney(tip);\n    totalAmountEl.textContent = formatMoney(total);\n    perPersonEl.textContent = formatMoney(perPerson);\n  }","replace":"    tipAmountEl.textContent = formatMoney(tip);\n    totalAmountEl.textContent = formatMoney(total);\n    perPersonEl.textContent = formatMoney(perPerson);\n\n    recordCalculation(bill, selectedTip, people, total, perPerson);\n  }\n\n  const HISTORY_KEY = 'tip-calculator-history';\n\n  function recordCalculation(bill, tipPercent, people, total, perPerson) {\n    if (bill <= 0 || people <= 0) return;\n\n    const historyEl = document.getElementById('history-list');\n    const clearBtn = document.getElementById('history-clear');\n    if (!historyEl || !clearBtn) return;\n\n    let history = [];\n    try {\n      history = JSON.parse(localStorage.getItem(HISTORY_KEY)) || [];\n    } catch (e) {\n      history = [];\n    }\n\n    history.unshift({\n      bill: bill.toFixed(2),\n      tip: tipPercent,\n      people,\n      total: total.toFixed(2),\n      perPerson: perPerson.toFixed(2),\n      time: new Date().toLocaleTimeString()\n    });\n\n    history = history.slice(0, 5);\n\n    try {\n      localStorage.setItem(HISTORY_KEY, JSON.stringify(history));\n    } catch (e) {}\n\n    renderHistory(history, clearBtn);\n  }\n\n  function renderHistory(history, clearBtn) {\n    const historyEl = document.getElementById('history-list');\n    if (!historyEl) return;\n\n    historyEl.innerHTML = '';\n\n    if (history.length === 0) {\n      const empty = document.createElement('li');\n      empty.className = 'history-empty';\n      empty.textContent = 'No calculations yet';\n      historyEl.appendChild(empty);\n    } else {\n      history.forEach(item => {\n        const li = document.createElement('li');\n        li.textContent = `Bill $${item.bill} + ${item.tip}% tip across ${item.people} people = $${item.total} total ($${item.perPerson} each) @ ${item.time}`;\n        historyEl.appendChild(li);\n      });\n    }\n\n    if (clearBtn) {\n      clearBtn.removeEventListener('click', clearHistory);\n      clearBtn.addEventListener('click', clearHistory);\n    }\n  }\n\n  function clearHistory() {\n    try {\n      localStorage.removeItem(HISTORY_KEY);\n    } catch (e) {}\n    renderHistory([], document.getElementById('history-clear'));\n  }\n\n  function initHistory() {\n    let history = [];\n    try {\n      history = JSON.parse(localStorage.getItem(HISTORY_KEY)) || [];\n    } catch (e) {\n      history = [];\n    }\n    renderHistory(history, document.getElementById('history-clear'));\n  }","find":"  if (tipButtons.length) tipButtons[0].classList.add('active');\n})();","replace":"  if (tipButtons.length) tipButtons[0].classList.add('active');\n\n  initHistory();\n})();"}]}
```
