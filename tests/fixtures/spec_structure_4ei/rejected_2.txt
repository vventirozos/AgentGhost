```json
{"files":[{"path":"index.html","append":"\n  <section class=\"history-panel\">\n    <div class=\"history-header\">\n      <h2>Recent Calculations</h2>\n      <button type=\"button\" id=\"clear-history\" class=\"clear-history\">Clear</button>\n    </div>\n    <ul id=\"history-list\" class=\"history-list\"></ul>\n  </section>\n  <script>\n    (function () {\n      const historyList = document.getElementById('history-list');\n      const clearBtn = document.getElementById('clear-history');\n      const STORAGE_KEY = 'tip-calc-history';\n\n      function getHistory() {\n        try {\n          return JSON.parse(localStorage.getItem(STORAGE_KEY)) || [];\n        } catch (e) {\n          return [];\n        }\n      }\n\n      function saveHistory(list) {\n        try {\n          localStorage.setItem(STORAGE_KEY, JSON.stringify(list));\n        } catch (e) {}\n      }\n\n      function formatHistoryItem(entry) {\n        const tipPct = entry.tip;\n        const bill = '$' + Number(entry.bill).toFixed(2);\n        const people = entry.people;\n        const total = '$' + Number(entry.total).toFixed(2);\n        const perPerson = '$' + Number(entry.perPerson).toFixed(2);\n        return 'Tip ' + tipPct + '% · $' + bill + ' / ' + people + ' ppl · Total ' + total + ' · $' + perPerson + ' each';\n      }\n\n      function render() {\n        const list = getHistory();\n        historyList.innerHTML = '';\n        if (list.length === 0) {\n          const li = document.createElement('li');\n          li.className = 'history-empty';\n          li.textContent = 'No calculations yet';\n          historyList.appendChild(li);\n          return;\n        }\n        list.forEach(function (entry) {\n          const li = document.createElement('li');\n          li.className = 'history-item';\n          li.textContent = formatHistoryItem(entry);\n          historyList.appendChild(li);\n        });\n      }\n\n      window.__recordTipCalculation = function (entry) {\n        const list = getHistory();\n        list.unshift(entry);\n        if (list.length > 5) list.length = 5;\n        saveHistory(list);\n        render();\n      };\n\n      if (clearBtn) {\n        clearBtn.addEventListener('click', function () {\n          localStorage.removeItem(STORAGE_KEY);\n          render();\n        });\n      }\n\n      render();\n    })();\n  </script>\n","edits":[{"find":"    <div class=\"results\">","replace":"    <div class=\"results\">\n      <div class=\"result-row\">\n        <span>Tip amount</span>\n        <span id=\"tip-amount\">$0.00</span>\n      </div>\n      <div class=\"result-row\">\n        <span>Total</span>\n        <span id=\"total-amount\">$0.00</span>\n      </div>\n      <div class=\"result-row\">\n        <span>Per person</span>\n        <span id=\"per-person\">$0.00</span>\n      </div>\n    </div>\n    <section class=\"history-panel\">\n      <div class=\"history-header\">\n        <h2>Recent Calculations</h2>\n        <button type=\"button\" id=\"clear-history\" class=\"clear-history\">Clear</button>\n      </div>\n      <ul id=\"history-list\" class=\"history-list\"></ul>\n    </section>\n    <script>\n      (function () {\n        const historyList = document.getElementById('history-list');\n        const clearBtn = document.getElementById('clear-history');\n        const STORAGE_KEY = 'tip-calc-history';\n\n        function getHistory() {\n          try {\n            return JSON.parse(localStorage.getItem(STORAGE_KEY)) || [];\n          } catch (e) {\n            return [];\n          }\n        }\n\n        function saveHistory(list) {\n          try {\n            localStorage.setItem(STORAGE_KEY, JSON.stringify(list));\n          } catch (e) {}\n        }\n\n        function formatHistoryItem(entry) {\n          const tipPct = entry.tip;\n          const bill = '$' + Number(entry.bill).toFixed(2);\n          const people = entry.people;\n          const total = '$' + Number(entry.total).toFixed(2);\n          const perPerson = '$' + Number(entry.perPerson).toFixed(2);\n          return 'Tip ' + tipPct + '% · $' + bill + ' / ' + people + ' ppl · Total ' + total + ' · $' + perPerson + ' each';\n        }\n\n        function render() {\n          const list = getHistory();\n          historyList.innerHTML = '';\n          if (list.length === 0) {\n            const li = document.createElement('li');\n            li.className = 'history-empty';\n            li.textContent = 'No calculations yet';\n            historyList.appendChild(li);\n            return;\n          }\n          list.forEach(function (entry) {\n            const li = document.createElement('li');\n            li.className = 'history-item';\n            li.textContent = formatHistoryItem(entry);\n            historyList.appendChild(li);\n          });\n        }\n\n        window.__recordTipCalculation = function (entry) {\n          const list = getHistory();\n          list.unshift(entry);\n          if (list.length > 5) list.length = 5;\n          saveHistory(list);\n          render();\n        };\n\n        if (clearBtn) {\n          clearBtn.addEventListener('click', function () {\n            localStorage.removeItem(STORAGE_KEY);\n            render();\n          });\n        }\n\n        render();\n      })();\n    </script>"}]},"verify":"node -e \"const {JSDOM}=require('jsdom');const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const dom=new JSDOM(html,{runScripts:'outside-only'});const w=dom.window;w.localStorage.setItem('tip-calc-history',JSON.stringify([{tip:10,bill:50,people:2,total:60,perPerson:30}]));w.__recordTipCalculation({tip:15,bill:100,people:4,total:115,perPerson:28.75});const list=w.document.getElementById('history-list');const items=[...list.children].map(c=>c.textContent);const ok=list.children.length===2&&items.some(t=>t.includes('15%'))&&items.some(t=>t.includes('$100.00'));process.exit(ok?0:1)\"","summary":"Added a persisted history section listing the last five tip calculations with a clear button, wired to app.js via window.__recordTipCalculation after each calculation.","ledger":"History is stored under localStorage key 'tip-calc-history' as an array of {tip,bill,people,total,perPerson}, capped at 5 entries, rendered by window.__recordTipCalculation()."}
```
