fastapi>=0.135.0
uvicorn>=0.40.0
python-multipart>=0.0.6
httpx[socks]>=0.24.0
PySocks>=1.7.1
apscheduler>=3.11.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
docker>=7.1.0
chromadb>=1.5.0
networkx>=3.0
PyMuPDF>=1.23.0
pypdf>=3.17.0
# Pure-Python markdown → HTML renderer used by tools/report_pdf.py to feed
# fitz.Story / fitz.DocumentWriter. No system deps. The tool degrades
# gracefully (wraps bodies in <pre>) if this is missing, but the output is
# much nicer with it present, so it's a hard requirement.
markdown>=3.5
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0
ddgs>=9.0.0
curl-cffi>=0.14.0
brotlicffi>=1.0.9
transformers>=4.35.0
torch>=2.0.0
sentence-transformers>=2.2.2
tiktoken>=0.5.0
sentencepiece>=0.2.0
huggingface-hub>=0.20.0
psutil>=5.9.0
qwen-agent>=0.0.30
# Transitive dependency of qwen-agent: `qwen_agent/utils/utils.py` does
# `import soundfile` at module load time. Without it, importing anything
# under `qwen_agent.*` (including `qwen_agent.tools.base` used by
# `tools/qwen_bridge.py`) fails with ModuleNotFoundError on the first
# call. The default agent path doesn't touch qwen_bridge, so the bug
# was dormant until someone used the `agent_qwen.py` Qwen variant or
# ran `tests/test_qwen*_bridge.py`. Pinned here so installs never
# leave the transitive dep unsatisfied. macOS / Windows: pip ships a
# prebuilt wheel with libsndfile bundled. Linux: also needs the
# system `libsndfile1` package (apt-get install libsndfile1).
soundfile>=0.12.0
# Chess move legality + FEN handling for the participant-mode game endpoint
# (api: POST /v1/game/move) — the agent plays the user itself; python-chess
# only validates/applies moves, it never chooses them.
python-chess>=1.999
slack-bolt>=1.27.0
slack-sdk>=3.21.0
tabulate>=0.10.0
black>=24.0.0
pylint>=3.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
dspy-ai>=3.2.0
