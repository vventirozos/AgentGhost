fastapi>=0.135.0
uvicorn>=0.40.0
python-multipart>=0.0.6
httpx[socks]>=0.24.0
PySocks>=1.7.1
apscheduler>=3.11.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
docker>=7.1.0
chromadb>=1.5.0
networkx>=3.0
PyMuPDF>=1.23.0
pypdf>=3.17.0
# Pure-Python markdown → HTML renderer used by tools/report_pdf.py to feed
# fitz.Story / fitz.DocumentWriter. No system deps. The tool degrades
# gracefully (wraps bodies in <pre>) if this is missing, but the output is
# much nicer with it present, so it's a hard requirement.
markdown>=3.5
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0
ddgs>=9.0.0
curl-cffi>=0.14.0
brotlicffi>=1.0.9
transformers>=4.35.0
torch>=2.0.0
sentence-transformers>=2.2.2
tiktoken>=0.5.0
sentencepiece>=0.2.0
huggingface-hub>=0.20.0
psutil>=5.9.0
# `qwen-agent` was REMOVED 2026-07-27 along with the `agent_qwen.py` /
# `tools/qwen_bridge.py` variant it existed for. That variant had zero
# instantiation sites — nothing ever constructed GhostQwenAgent — so the
# dependency (and its heavy transitive tree) was pure install cost, while
# the bridge itself carried two latent defects (a cross-loop
# `_run_coro_blocking` that would freeze the whole agent, and unguarded
# kwarg collisions). The live agent runs `core/agent.py`, which speaks to
# llama-server over the OpenAI-compatible API and needs none of it.
#
# Read audio/waveform I/O. Now required only by the TTS/STT voice-server
# external (`interface/externals/tts_stt/voice_server.py`), NOT by
# qwen-agent — keep it pinned for that deploy. macOS / Windows: pip ships a
# prebuilt wheel with libsndfile bundled. Linux: also needs the system
# `libsndfile1` package (apt-get install libsndfile1).
soundfile>=0.12.0
# Chess move legality + FEN handling for the participant-mode game endpoint
# (api: POST /v1/game/move) — the agent plays the user itself; python-chess
# only validates/applies moves, it never chooses them.
python-chess>=1.999
slack-bolt>=1.27.0
slack-sdk>=3.21.0
tabulate>=0.10.0
black>=24.0.0
pylint>=3.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
dspy-ai>=3.2.0
